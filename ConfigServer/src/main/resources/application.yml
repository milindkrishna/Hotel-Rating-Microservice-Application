server:
  port: 8085

spring:
  application:
    name: CONGIF-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/milindkrishna/microservice-config
          clone-on-start: true